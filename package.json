{
  "name": "aragon-be",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config build/index.js",
    "build:server": "node --max-old-space-size=4096 ./node_modules/.bin/tsc -p .",
    "start:dev": "DOTENV_CONFIG_PATH=.env.local nodemon -r dotenv/config --watch \"src/**/*.ts\" --exec \"ts-node\" --files \"src/index.ts\" --signal SIGKILL",
    "copy-files": "copyfiles -u 1 ./src/**/*.html build/src/",
    "build": "rm -rf build && tsc -p .",
    "dev": "NODE_ENV=development concurrently \"yarn start:dev\"",
    "predeploy:prod": "./scripts/predeploy.sh && yarn build",
    "deploy:prod": "NODE_ENV=production node build/index.js",
    "pretty": "prettier --write ./src/**/*.ts",
    "configure-husky": "npx husky install && npx husky add .husky/pre-commit \"npx --no-install lint-staged\"",
    "deploy:dev": "tsc && node build/src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^8.2.3",
    "@types/express": "^5.0.3",
    "@types/morgan": "^1.9.10",
    "@types/uuid": "^11.0.0",
    "concurrently": "^9.2.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "joi": "^18.0.1",
    "moment": "^2.30.1",
    "morgan": "^1.10.1",
    "pg": "^8.16.3",
    "pino": "^10.0.0",
    "sqlutils": "^1.5.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
